// Firebase Configuration
//
// This app works in LOCAL-ONLY mode by default (no Firebase required).
// All data is stored in your browser's localStorage.
//
// To enable cloud features (optional):
// 1. Set up a Firebase project at https://console.firebase.google.com/
// 2. Copy firebase-config.template.js to firebase-config.js (or edit this file)
// 3. Replace the values below with your Firebase project credentials
// 4. NEVER commit your actual Firebase credentials to version control
//
// See SECURITY.md for detailed setup instructions.

// Uncomment and fill in your Firebase configuration:
/*
const firebaseConfig = {
    apiKey: "your-api-key-here",
    authDomain: "your-project-id.firebaseapp.com",
    projectId: "your-project-id",
    storageBucket: "your-project-id.appspot.com",
    messagingSenderId: "123456789012",
    appId: "1:123456789012:web:abcdef123456789012345678",
    measurementId: "G-XXXXXXXXXX"
};

window.firebaseConfig = firebaseConfig;
*/

// By default, firebaseConfig is undefined (local-only mode)
window.firebaseConfig = undefined;
